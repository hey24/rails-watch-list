<div class="container">
  <h1>Lists</h1>

  <% @lists.each do |list| %>
    <p><%= link_to list.name, list_path(list) %></p>
  <% end %>

  <%= link_to "Create New List", new_list_path, class: "btn btn-primary" %>
</div>
